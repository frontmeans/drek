import { NamespaceDef } from '@frontmeans/namespace-aliaser';

/**
 * Default Drek namespace definition.
 */
export const Drek__NS = (/*#__PURE__*/ new NamespaceDef(
    'https://frontmeans.github.io/ns/drek',
    'drek',
));
